{{
  "framework": null,
  "buildCommand": "curl -L https://github.com/gohugoio/hugo/releases/download/v0.128.0/hugo_extended_0.128.0_linux-amd64.tar.gz | tar -xz && ./hugo --gc --minify --baseURL https://$VERCEL_URL",
  "outputDirectory": "public",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./"
}
